export interface WorkshopBadgeData {
  orgType: string;       // Q1: org they're part of
  schedule: string;      // Q2: Early Bird | Night Owl
  chartType: string;     // Q3: chart type they identify with
  comfortScore: number;  // Q4: 0–10 data viz comfort scale
  role: string;          // Q5: Analyst | Designer | Developer | Researcher
}
